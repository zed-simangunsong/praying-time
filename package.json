{
  "name": "praying-time",
  "version": "1.0.0",
  "description": "Praying time based on area and subscriptions",
  "main": "index.js",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded public/scss:public/css",
    "sass-prod": "sass --no-source-map --style=compressed public/scss:public/css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.76.0"
  }
}
